const galeria = document.getElementById('gallery');

const monitos = galeria.children;

monitos.style.transition = 'all .5s';
monitos.style.transform = 'rotate(25def)';
monitos.style.transform = 'scale(1.4)';
